External libraries
------------------

Folders
* iniparser - parameter parser for .ini files
* libfreenect - open source Kinect library


NOTES:

libfreenect:
  To build:
  1) mkdir libfreenect/build
  2) cd libfreenect/build
  3) cmake ..
  4) make

BECAUSE I FORGET: These two commands will pull down the 
   submodule repositories.  The first clone won't automagically
   grab the submodule codes

git submodule init
git submodule update
